/* eslint-disable no-undef */
module.exports = {
    VERBOSE: __ENV.VERBOSE ? true : false,
    DEBUG_ALL: __ENV.DEBUG_ALL ? true : false,
    POSITIVE_CASE: __ENV.ONLY_POSITIVE ? true : false,
    SKIP_PREP_DATA: __ENV.SKIP_PREP_DATA ? true : false,
}